# `@jesset/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ npm install --save-dev @jesset/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@jesset/prettier-config"
}
```
